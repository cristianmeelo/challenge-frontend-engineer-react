/**
 * Enum representing the types for the EditAssignedUserModal.
 * @enum {string}
 * @readonly
 * @enum {EDIT_ASSIGNED_USER_MODAL_TYPES}
 */
export enum EDIT_ASSIGNED_USER_MODAL_TYPES {
  /**
   * Represents the type 'asset'.
   */
  ASSET = 'asset',

  /**
   * Represents the type 'workorder'.
   */
  WORKORDER = 'workorder',
}
